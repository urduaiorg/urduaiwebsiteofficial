# Content-hashed font filenames: changed bytes always receive a new URL.
<IfModule mod_headers.c>
  <FilesMatch "-[a-f0-9]{12}\.woff2$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>
</IfModule>
