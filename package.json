{
  "name": "repo",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "postbuild": "pagefind --site dist",
    "validate:redirects": "python3 scripts/validate_redirects.py",
    "validate:redirects:live": "python3 scripts/validate_redirects.py --base-url https://urduai.org",
    "learn-links": "node scripts/add-blog-learn-links.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.0",
    "@astrojs/sitemap": "^3.0.0",
    "astro": "^6.1.3"
  },
  "devDependencies": {
    "pagefind": "^1.4.0"
  }
}
